export function tryResolveMenuItemAsCustomArgument(extension: Extension, menuResult: any): any;
/**
 * An extension instance
 *
 * It could be a "classic" scratch extension or a PRG-framework-based extension (with or without custom argument support)
 */
export type Extension = object;
