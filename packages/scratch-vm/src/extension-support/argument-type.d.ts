export = ArgumentType;
/**
 * Block argument types
 */
type ArgumentType = string;
declare namespace ArgumentType {
    const ANGLE: string;
    const BOOLEAN: string;
    const COLOR: string;
    const NUMBER: string;
    const STRING: string;
    const MATRIX: string;
    const NOTE: string;
    const IMAGE: string;
}
