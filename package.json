{
  "name": "root",
  "private": true,
  "scripts": {
    "ts:node": "./node_modules/.bin/ts-node -r tsconfig-paths/register",
    "ts:node:extensions": "npm run ts:node -- --project ./extensions/scripts/tsconfig.json",
    "init": "npm install && npm install --prefix ./extensions/ && npx lerna bootstrap --force-local",
    "init:build": "npm install && npm install --prefix ./extensions/ && npx lerna link --force-local && npx lerna exec -- npm install && npx lerna link --force-local",
    "clean:build": "rm -rf ./build",
    "move:build:to:root": "mv ./packages/scratch-gui/build ./build",
    "invalidate:node:modules": "rm -rf node_modules && rm -rf packages/**/node_modules",
    "invalidate:package:locks": "rm -f package-lock.json && rm -f packages/**/package-lock.json",
    "clean:init": "npm run invalidate:node:modules && npm run invalidate:package:locks && npm run init",
    "build:extensions": "npm run ts:node:extensions -- ./extensions/scripts/bundle.ts watch=false",
    "build:gui": "npm run build --prefix ./packages/scratch-gui/",
    "build": "npm run init:build && npm run clean:build && npm run build:extensions && npm run build:gui && npm run move:build:to:root",
    "dev": "npm run ts:node:extensions -- ./scripts/dev.ts",
    "new:extension": "npm run ts:node:extensions -- ./extensions/scripts/factories/extension.ts",
    "add:ui": "npm run ts:node:extensions -- ./extensions/scripts/factories/ui.ts",
    "rebuild:blocks": "npm run prepublish --prefix ./packages/scratch-blocks && npm run dev"
  },
  "devDependencies": {
    "@types/webgl2": "^0.0.6",
    "lerna": "^3.20.2",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.1.1",
    "tslib": "^2.4.1",
    "typescript": "^4.9.4"
  }
}
