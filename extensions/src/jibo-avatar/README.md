# Jibo Avatar

Renders the Virtual Jibo avatar on the Scratch stage.

## Run
```bash
cd ~/MiT/virtual-scratch/prg-raise-playground && pnpm --filter prg-extension-root dev --include jibo-avatar
```

## Blocks
- Reset Jibo pose
- Rotate Jibo by degrees
- Set Jibo eye color (r, g, b)

