{
  "name": "scratch3_jibo-extension",
  "version": "1.0.0",
  "description": "An extension created using the PRG AI Blocks framework",
  "main": "index.ts",
  "scripts": {
    "directory": "echo scratch3_jibo",
    "test": "pnpm --filter prg-extension-root test scratch3_jibo/index.test.ts",
    "dev": "pnpm --filter prg-extension-root dev --include scratch3_jibo",
    "add:ui": "pnpm --filter prg-extension-root add:ui scratch3_jibo",
    "add:arg": "pnpm --filter prg-extension-root add:arg scratch3_jibo"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^11.3.1",
    "roslib": "^1.4.1"
  }
}